{{!-- this renders your information on your user profile --}}
{{#each Recipe}}
<div class="card">
    <div class="card-header" id="headingOne">
        <h2 class="mb-0">
            <button class="btn btn-link" type="button" data-toggle="collapse" data-target="#collapse{{id}}"
                aria-expanded="true" aria-controls="collapseOne">
                <h3>{{recipeName}}</h3>
            </button>
        </h2>
    </div>

    <div id="collapse{{id}}" class="collapse show" aria-labelledby="headingOne" data-parent="#accordionExample">
        <div class="card-body">
            <div>
                <p>{{description}}</p>
                <p>{{instructions}}</p>
                <p>{{ingredients}}</p>
                <p>{{chefComments}}</p>
                <p>{{Rating}}</p>
                <p>{{Category}}</p>
            </div>
        </div>
    </div>
</div>
{{/each}}